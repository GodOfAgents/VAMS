# VAMS Pre-Testnet Risk Register

**Baseline commit:** Update this field when evidence is generated.  
**Policy:** A blocked track prevents testnet readiness regardless of CI health.
**Last verified:** 2026-07-13

| Risk | Track | Severity | Current evidence | Required closure |
| --- | --- | --- | --- | --- |
| AR-001 Legacy deployment and governance profile | T16, T21 | Critical | Legacy broad scripts fail closed. Polygon scripts now chain-lock Amoy and bind each 3-of-5/3-of-5/2-of-3 Safe to proxy/singleton runtime code, the exact compiled VAMS timelock, role transfers, and deployer-role removal. Network-specific schemas bind equivalent Cardano script/multisig evidence. No real authority instance or rehearsal artifact is present. | Execute both rehearsals against known authorities, independently observe code/script identity and roles, review broadcast bundles, and record transactions and rollback evidence. |
| AR-002 Live DA and proof substitution | T13, T30 | High | Celestia no longer falls back from live errors to mock receipts and requires exact retrieval. Near non-mock submission is disabled until signed submission/retrieval exists. Mock receipts, Avail, EigenDA, and current VDSO DA routes remain release-ineligible. | Prove real Celestia and Near submissions, provider-native inclusion, independently observed exact retrieval, and outage behavior without mock fallback. |
| AR-003 Telemetry and memory privacy | T26, T33 | High | DA KPIs use pseudonymized identifiers and bounded scalar schemas. An AST-derived inventory covers DA, Sentinel, and VDSO publishers, and v2 review evidence must bind retention, redaction, public-content, and publisher artifacts. SHA-256 pseudonyms remain linkable and no human approval is present. | Approve the complete inventory and minimization analysis, execute redaction/deletion tests, and obtain named privacy/public-content review with zero blockers. |
| AR-004 Incomplete current-commit assurance | T04-T12 | High | Local Foundry passes 693/693, Python passes 684/684, Aiken passes 50 definitions over 1,750 property iterations, Linux Rust passes 34/34, pip-audit and Slither fail-high pass, and broad Semgrep scans report zero findings. Seventeen Semgrep timeout pairs received separate direct source adjudication, but the tree is dirty and signed exact-commit evidence is absent. | Rerun every gate on the exact clean commit in capable CI, obtain external acceptance of the scanner adjudications, expand Aiken transaction-state properties, and retain signed manifests before changing track status. |
| AR-005 Incomplete economic execution paths | T18, T23, T29 | High | Incomplete interrupt and Arweave upload paths are rejected in live environments. TEE/ZK proofs require real verifier callbacks and no longer pass on shape. Testnet staking starts at a zero reward rate and the staking contract receives no minter authority. | Keep excluded capabilities out of the live graph; independently test any future implementation for authorization, idempotency, replay, timeout, recovery, and reward solvency before governance enables it. |
| AR-006 Frontend wallet and origin trust | T08, T32, T34 | High | Frontend gateway origin is environment-bound and HTTPS-only in production; CSP removes inline scripts/external fonts; production build and npm audit pass. The first testnet profile is explicitly read-only with wallet transactions disabled. | Pass browser CSP/XSS checks, phishing review, and accessibility tests before enabling any wallet path. |
| AR-007 Historical credential exposure | T09, T16, T21 | Critical | PR 4 run `29611633518` reports 869 Gitleaks findings across 65 scanned commits and 20 unverified TruffleHog findings. Three PEM occurrences map to two permanently decommissioned identities; Infura is revoked with the exact revocation time explicitly unavailable. Two narrow rule-targeted public-value allowlists are regression-bound; no blanket baseline exists. | Land the incident preparation PR, rewrite every affected ref on an encrypted disposable mirror, coordinate GitHub cache/PR cleanup and collaborator reclones, then require zero complete-history Gitleaks and all-category TruffleHog findings plus a schema-v4 closure report. |
| AR-007 Build-action and dependency provenance | T05, T07-T09 | Medium | All 36 external workflow actions use reviewed 40-character commit pins, Python security tools are version-pinned, and the workflow validator passes. The VIR job also pins Rust 1.92. Transitive artifact provenance, lockfile review, signed SBOMs, and signed aggregate manifests remain incomplete. | Emit tool versions in commit-bound evidence, verify lockfiles and downloaded artifacts, and sign the SBOM plus audit manifests. |
| AR-008 Market concentration and capture | T19-T22 | High | Regional/insurance caps exist. A seeded 100,000-epoch synthetic campaign detected 20,000 instances of each linked-reward, linked-capacity, regional-capture, thin-liquidity, and operator-linked wash-return class with zero misses and zero baseline false positives. Live beneficial-owner and governance-capture evidence is incomplete. | Run the analyzer against signed operator attestations, add governance/settlement state-machine simulations, and enforce stop conditions before provider rewards or governance are enabled. |
| AR-009 VDSO dual-host authority conflict | VDSO canary foundation | Critical | Rust/Python intents and Polygon contracts bind state domain, host, and monotonic authority epoch; Aiken independently checks the wire mapping. The Polygon kernel now rejects all Cardano-authoritative writes, leaving native `READ`/`ACCUMULATE` conformance to Aiken. No deployed authority record, two-tool migration-root ceremony, or rollback rehearsal exists. | Independently reproduce migration roots, rehearse governance checkpoint handoff and stale-authority rejection on both testnets, and prove rollback cannot create dual writers before any authoritative migration. |
| AR-010 VDSO reservation split-brain | VDSO canary foundation | Critical | Polygon contracts bind reservations to domain, intent, holder, authority epoch, and monotonic fence. Timeout enters `RECOVERY_PENDING` without unlock; late commit reuses semantic/adapter/proof verification, and abort requires an injected non-execution verifier. The deployment rehearsal deliberately configures no abort verifier, so ambiguity freezes closed. Cardano value-bearing reservation remains disabled. | Supply and independently audit a real destination non-execution verifier, run delayed-finality/adversarial state-machine tests, retain exact-commit proof evidence, and keep value-bearing domains unassigned until closure. |
| AR-011 VDSO privacy and PQ overclaim | VDSO canary foundation | High | XChaCha20-Poly1305 ciphertext sidecars, unique recipient envelopes, signed-root/content-hash joins, plaintext-rejection tests, and separate semantic/settlement types exist. RFC 9180 HPKE remains an injected classical dependency and Tier 2 binds secp256k1 plus ML-DSA-65, but the Gateway has no reviewed ML-DSA verifier and therefore rejects Tier 2. SP1/RISC Zero adapters are disabled placeholders, not PQ evidence. | Complete independent privacy/cryptography review, audited HPKE and ML-DSA integrations with NIST vectors, metadata-leakage and downgrade tests, real proof-backend review, and separate authorization/proof/settlement evidence before promotion. |
| AR-012 Upgradeable adapter/verifier substitution | VDSO canary foundation | Critical | Registries pin the runtime address codehash, which does not pin an ERC-1967/beacon proxy implementation. Deployment activates no adapter or verifier, so there is no present live exposure. | Permit only independently reviewed immutable direct deployments until the registry can bind and continuously verify implementation identity/codehash; reject proxy activation and rehearse quarantine on any identity drift. |

Risk closure requires a linked finding record, remediation commit, regression
test, independent verification, and evidence manifest. Editing this register
alone never changes a track to `verified`.
