# VAMS Pre-Testnet Risk Register

**Baseline commit:** Update this field when evidence is generated.  
**Policy:** A blocked track prevents testnet readiness regardless of CI health.

| Risk | Track | Severity | Current evidence | Required closure |
| --- | --- | --- | --- | --- |
| AR-001 Legacy deployment and governance profile | T16, T21 | Critical | Legacy broad scripts fail closed. `DeployTestnet.s.sol` now chain-locks Amoy, validates 3-of-5 and 2-of-3 Safes, enforces 48 hours, sends supply to the treasury Safe, and verifies deployer role removal. | Execute a Safe-backed rehearsal, review the broadcast bundle, verify bytecode, and record owners, thresholds, role-transfer transactions, and rollback evidence. |
| AR-002 Live DA and proof substitution | T13, T30 | High | Live audit logs now include only Celestia/Near by default; enabling Avail/EigenDA stubs raises, and disabled explicit targets fail rather than reroute. | Prove real enabled-DA submission/retrieval receipts and test outage behavior without accepting mock evidence. |
| AR-003 Telemetry and memory privacy | T26, T33 | High | DA KPIs and telemetry are schema-allowlisted. S-MMU confines paths, restricts HIPIF deletion, requires a trusted reviewer-authorizer callback for EvoMem persistence/L3 purge, hashes access addresses, and provides a session hard reset. | Verify every remaining publisher/log sink, approve retention/deletion policy, and complete public-document PII/partner review. |
| AR-004 Incomplete current-commit assurance | T04-T12 | High | Post-change local aggregates pass 643 Solidity and 569 Python tests; Aiken passes 33 unit tests plus 7 properties over 1,750 generated cases. Bandit, pip-audit, Semgrep, Slither high-impact, frontend build/audit, workflow, and first-party policy gates pass locally. The dirty tree means none of this is exact-commit release evidence; independent review, transaction-level Aiken properties, signed SBOM, signed aggregate evidence, Gitleaks, and TruffleHog remain incomplete. | Commit the reviewed diff, rerun every gate on that clean commit, independently review Slither adjudications, expand Aiken coverage to transaction state machines, and retain the signed manifest; close every current finding before changing track status. |
| AR-005 Incomplete economic execution paths | T18, T23, T29 | High | Incomplete interrupt and Arweave upload paths are rejected in live environments. TEE/ZK proofs require real verifier callbacks and no longer pass on shape. Testnet staking starts at a zero reward rate and the staking contract receives no minter authority. | Keep excluded capabilities out of the live graph; independently test any future implementation for authorization, idempotency, replay, timeout, recovery, and reward solvency before governance enables it. |
| AR-006 Frontend wallet and origin trust | T08, T32, T34 | High | Frontend gateway origin is environment-bound and HTTPS-only in production; CSP removes inline scripts/external fonts; production build and npm audit pass. The first testnet profile is explicitly read-only with wallet transactions disabled. | Pass browser CSP/XSS checks, phishing review, and accessibility tests before enabling any wallet path. |
| AR-007 Build-action and dependency provenance | T05, T07-T09 | Medium | All 35 external workflow actions use reviewed 40-character commit pins, Python security tools are version-pinned, and the workflow validator passes. Transitive artifact provenance, lockfile review, signed SBOMs, and signed aggregate manifests remain incomplete. | Emit tool versions in commit-bound evidence, verify lockfiles and downloaded artifacts, and sign the SBOM plus audit manifests. |
| AR-008 Market concentration and capture | T19-T22 | High | Regional/insurance caps exist. A seeded 100,000-epoch synthetic campaign detected 20,000 instances of each linked-reward, linked-capacity, regional-capture, thin-liquidity, and operator-linked wash-return class with zero misses and zero baseline false positives. Live beneficial-owner and governance-capture evidence is incomplete. | Run the analyzer against signed operator attestations, add governance/settlement state-machine simulations, and enforce stop conditions before provider rewards or governance are enabled. |

Risk closure requires a linked finding record, remediation commit, regression
test, independent verification, and evidence manifest. Editing this register
alone never changes a track to `verified`.
